OAUTH_SECRET=generate-a-random-secret-with-at-least-32-characters
# Optional single-owner private mode; use at least 16 random characters:
# MCP_AUTH_PASSWORD=replace-with-a-private-access-password
# Vercel injects this after a Blob Store is connected:
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
# MCP_RESOURCE_URL=https://custom-domain.example.com
